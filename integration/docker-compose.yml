version: '3.1'

services:
    generator:
        image: docker-registry:443/aspnetcore/gen:ci-${GENERATOR_BUILD_NUMBER-20}
    #    ports:
    #        - 8082:80
    mail:
        image: mailhog/mailhog
    #    ports:
    #        - "8025:8025"
    integration:
        image: docker-registry:443/aspnetcore/gen:integration-${BUILD_NUMBER-20}
        build: .
        environment: 
            - TEAMCITY_PROJECT_NAME